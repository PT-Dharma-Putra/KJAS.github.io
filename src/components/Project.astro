---
import { projects } from "../data/projects";
---

<section
  id="project"
  class="relative py-24 bg-zinc-900 text-white overflow-hidden"
>
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0 z-0">
    <img
      src="/images/hero-bg.png"
      alt="Background"
      class="w-full h-full object-cover opacity-30 mix-blend-overlay"
    />
    <div
      class="absolute inset-0 bg-gradient-to-b from-zinc-900/80 via-zinc-900/50 to-zinc-900"
    >
    </div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-6">
    <div class="text-center mb-16">
      <h2
        class="font-display text-5xl md:text-6xl text-white font-normal uppercase tracking-wide mb-2"
      >
        Our<br /><span class="font-bold">Project KJAS</span>
      </h2>
      <!-- <h3 class="font-display text-4xl md:text-5xl font-bold text-white mb-4">
        KJAS
      </h3> -->
      <p class="font-serif italic text-xl text-white/80">
        sofa, chair, table & bench
      </p>
    </div>

    <div class="swiper project-slider !overflow-visible pb-12">
      <div class="swiper-wrapper">
        {
          projects.map((project) => (
            <div class="swiper-slide h-auto">
              <a
                href={`/project/${project.slug}`}
                class="block group relative h-[500px] w-full overflow-hidden border-2 border-black/20 cursor-pointer"
              >
                <img
                  src={project.img}
                  alt={project.name}
                  class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent" />
                <div class="absolute bottom-0 left-0 w-full p-8 flex justify-between items-end">
                  <h4 class="font-display text-2xl font-bold leading-tight uppercase w-2/3">
                    {project.name}
                  </h4>
                  <div class="w-10 h-10 rounded-full border border-white flex items-center justify-center group-hover:bg-white group-hover:text-black transition-colors">
                    <span class="material-icons-outlined text-sm">
                      arrow_forward
                    </span>
                  </div>
                </div>
              </a>
            </div>
          ))
        }
      </div>

      <!-- Navigation Buttons -->
      <div
        class="swiper-button-prev !left-4 !md:-left-12 !text-white bg-white/10 backdrop-blur-md border border-white/20 p-8 rounded-full hover:bg-white hover:text-zinc-900 transition-all shadow-2xl flex items-center justify-center group pointer-events-auto"
      >
        <span
          class="material-icons-outlined !text-3xl group-hover:scale-110 transition-transform"
          >keyboard_arrow_left</span
        >
      </div>
      <div
        class="swiper-button-next !right-4 !md:-right-12 !text-white bg-white/10 backdrop-blur-md border border-white/20 p-8 rounded-full hover:bg-white hover:text-zinc-900 transition-all shadow-2xl flex items-center justify-center group pointer-events-auto"
      >
        <span
          class="material-icons-outlined !text-3xl group-hover:scale-110 transition-transform"
          >keyboard_arrow_right</span
        >
      </div>
    </div>

    <div class="text-center mt-12 max-w-3xl mx-auto">
      <p class="text-lg md:text-xl font-light leading-relaxed text-white/90">
        Percayakan kebutuhan furnitur Anda kepada ahlinya. Lihat portofolio kami
        dan temukan kualitas terbaik furnitur kustom untuk hotel, klinik, dan
        lainnya.
      </p>
    </div>
  </div>
</section>

<script>
  import Swiper from "swiper";
  import { Navigation, Autoplay } from "swiper/modules";
  import "swiper/css";
  import "swiper/css/navigation";

  const initSwiper = () => {
    new Swiper(".project-slider", {
      modules: [Navigation, Autoplay],
      loop: true,
      centeredSlides: true,
      slidesPerView: 1,
      spaceBetween: 20,
      autoplay: {
        delay: 4000,
        disableOnInteraction: false,
      },
      navigation: {
        nextEl: ".project-slider .swiper-button-next",
        prevEl: ".project-slider .swiper-button-prev",
      },
      breakpoints: {
        640: {
          slidesPerView: 2.2,
          spaceBetween: 30,
          centeredSlides: false,
        },
        1024: {
          slidesPerView: 3,
          spaceBetween: 40,
          centeredSlides: false,
        },
      },
    });
  };

  document.addEventListener("astro:page-load", initSwiper);
  // Initial call just in case
  initSwiper();
</script>

<style>
  .swiper-button-next::after,
  .swiper-button-prev::after {
    display: none !important;
  }

  @media (max-width: 768px) {
    .swiper-button-next,
    .swiper-button-prev {
      display: none !important;
    }
  }
</style>
